python3.10 -m venv .venv
pip freeze
source .venv/bin/activate

docker-compose up -d
# run the "script recipe" in docker-compose.yml to run your containers

docker ps -a
docker remove ID
docker stop ID
docker restart metabase

# start Airbyte
./run-ab-platform.sh
#VER LINHA 393

sudo docker network inspect bigquery_default

sudo docker logs -f metabase

sudo docker-compose down --remove-orphans

docker stop $(docker ps -q)

docker exec -it metabase /bin/bash

# stop all dockers

# airbyte
# In your browser, visit http://localhost:8000
# username airbyte and password password

# cloudBeaver 
# cbadmin 12345678aA

# postgres
# postgres 123

# ClickHouse
# default default
# as vezes nao tem senha